ObjFileRenderer = function(objFileURL, width, height, domElement) {
    // Will render the provided objFile in a canvas of the specified dimensions added as a child of domElement
    // I believe objFileURL can be a local URL (e.g file://) but this might not be possible - assume it can be for now

}
