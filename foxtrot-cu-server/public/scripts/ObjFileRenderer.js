ObjFileRenderer = function(objData, width, height, domElement) {
    // Will render the provided objData in a canvas of the specified dimensions added as a child of domElement
    console.log(objData);
}
