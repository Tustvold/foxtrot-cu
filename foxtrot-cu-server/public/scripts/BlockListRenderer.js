
BlockListRenderer = function(width, height, domElement) {
    // Will create a canvas of the specified dimensions added as a child of domElement

    this.setBlockList = function(blockList) {
        // Will set this element to render the provided blocklist
    }

    this.onBlockSelected = function(block) {

    }
}
